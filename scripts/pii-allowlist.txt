# PII allowlist for scripts/validate_pii.py
#
# One confirmed-safe value per line. Lines starting with '#' and blank lines
# are ignored. Allowlisted values are skipped for every PII check (UUIDs,
# email addresses, and private IPs).
#
# Only add a value here after confirming it is a synthetic example and NOT a
# real Azure subscription/tenant/object ID, real email address, or real host.
#
# Examples:
#   00000000-0000-0000-0000-000000000000
#   svc-account@acme.example

# Storage Queue message ID in an `az storage message put` example output
# (docs/language-guides/nodejs/tutorial/premium/07-extending-triggers.md).
# An ephemeral per-message identifier generated by Azure Queue Storage, not an
# account/tenant/subscription identifier.
32d6a0d5-d2c0-426a-8516-96f1d63f5337
